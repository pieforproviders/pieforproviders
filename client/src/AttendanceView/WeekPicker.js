import React from 'react'
import { Button } from 'antd'

export function WeekPicker() {
  return (
    <div>
      <Button>back</Button>
      <Button disabled>dates</Button>
      <Button>forward</Button>
    </div>
  )
}
