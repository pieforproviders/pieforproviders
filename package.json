{
  "name": "pieforproviders",
  "version": "1.0.0",
  "description": "A digital assistant for your child care business.",
  "main": "index.js",
  "repository": "git@github.com:pieforproviders/pieforproviders.git",
  "author": "Kate Donaldson <kate@katelovescode.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "echo 'I am building!' && cd client && yarn install && yarn build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "postinstall": "yarn build && yarn deploy && echo 'Client built!'"
  },
  "engines": {
    "yarn": "1.21.1",
    "npm": "6.13.4",
    "node": "12.14.1"
  }
}
